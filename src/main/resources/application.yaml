spring:
  application:
    name: monolotic-app-backend
  datasource:
    url: jdbc:postgresql://localhost:5432/monolotic-app-db
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver

